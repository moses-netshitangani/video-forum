import React, {useEffect} from 'react';
import "../style/quiz.css";

const Quiz = props => {

    return(
        <div className={`quiz-cover ${props.cquiz}`}>
            Quiz here
        </div>
    )
}

export default Quiz;